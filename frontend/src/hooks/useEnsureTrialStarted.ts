/** Trial removed — no-op hook kept for compatibility with existing imports. */
export function useEnsureTrialStarted(
  _user: unknown,
  _onUpdated?: (updated: Record<string, unknown>) => void
) {}
